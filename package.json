{
  "name": "scratch-optimizer",
  "author": "Aliasfox",
  "license": "MIT",
  "version": "0.6.0",
  "description": "A browser-based tool to optimize Scratch (.sb3) projects",
  "scripts": {
    "dev": "npm install && npx serve public",
    "start": "npx serve public",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "keywords": [
    "scratch",
    "optimizer",
    "sb3",
    "turbowarp"
  ],
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
